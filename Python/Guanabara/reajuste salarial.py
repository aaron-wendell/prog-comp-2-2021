nome = input('Digite o nome do funcionario: ')
salario = float(input(f'Digite o salario de {nome}: '))
reajuste = salario * 1.15
print(f'O salario do funcionario {nome} é {salario} e com o reajuste de 15% sera {reajuste}')