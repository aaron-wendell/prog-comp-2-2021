let ex5 = () => {

    let produto = [50]
    for(i=0; i<3; i++){
        let objeto = {
        codigo: Number(prompt(`Digite o codigo do produto`)),
        desc: prompt(`Digite a descricao do produto`),
        valor: Number(prompt(`Digite o valor do produto`)),
        quantidade: Number(prompt(`Digite a quantidade do produto em estoque`))
        }
        console.push(objeto)
    }
    console.log(produto)

    consultaproduto(produto) => {
        do{
            switch(opcoes){
                case 1: 
            }
        }
        while(opcoes!=5)
    }
    consultaproduto(produto)
}

let consulta = (produto) => {
    
}